server:
  port: 8084

spring:
  application:
    name: sala-service
  datasource:
    url: jdbc:postgresql://db-sala:5432/sala_db
    username: admin
    password: admin
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
