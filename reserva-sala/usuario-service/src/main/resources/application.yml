server:
  port: 8083

spring:
  application:
    name: usuario-service
  datasource:
    url: jdbc:postgresql://db-usuario:5432/usuario_db
    username: admin
    password: admin
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
